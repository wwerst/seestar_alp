@../AGENTS.md

## Agent Model Policy
- All agents and subagents (foreground and background) — including Agent-tool subagents and Workflow/ultracode agents — must run on Claude Opus (pass `model: 'opus'` / `model: "opus"`) unless the user explicitly requests a different model.
